<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GerenciadorDespesas">

        <!-- Tela Inicial / Dashboard -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

        <!-- Tela Histórico Completo -->
        <activity
            android:name=".HistoricoCompletoActivity"
            android:exported="false" />

        <!-- Tela Resumo Mensal -->
        <activity
            android:name=".ResumoMensalActivity"
            android:exported="false" />

        <!-- Tela Sobre / Saber Mais -->
        <activity
            android:name=".SobreActivity"
            android:exported="false" />

        <!-- Tela Gráfico -->
        <activity
            android:name=".GraficoActivity"
            android:exported="false" />

        <!-- Tela Adicionar Despesa -->
        <activity
            android:name=".AddActivity"
            android:exported="false" />

        <!-- Tela Lista de Despesas -->
        <activity
            android:name=".ListaActivity"
            android:exported="false" />

        <!-- Tela Principal de Lançamento -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
